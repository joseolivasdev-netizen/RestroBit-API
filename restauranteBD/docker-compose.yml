version: '3.8'

services:
  db:
    image: postgres:15
    container_name: restrobit_postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 425369
      POSTGRES_DB: restaurante_bd
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: